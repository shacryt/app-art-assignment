import React, { useState, useEffect, Fragment } from 'react';
import styles from '../styles/components/SideMenu.module.scss';

const SideMenu = () => {
  return <div className={'fl-column ' + styles.wrapper} id='side-menu'></div>;
};

export default SideMenu;
